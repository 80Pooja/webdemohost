pipeline {
    agent any
    stages {
         stage{ label 'slave1' }
        stages {
            stage('Example') {
             steps {
                 echo 'Hello World'
             }
         }
    }
}
