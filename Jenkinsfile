pipeline {
         agent { label 'slave4' }
        stages {
           stage('Example') {
                    steps {
                             echo 'Hello World'
                    }
           }
    }
}
