pipeline {
         agent { label 'slave3' }
        stages {
           stage('Example') {
                    steps {
                             echo 'Hello World'
                    }
           }
    }
}
